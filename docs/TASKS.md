# TASKS.md — Prioritized Backlog (deadline: submission June 15)

Status legend: `[ ]` open · `[~]` in progress · `[x]` done · `[!]` blocked (note why in status file).
Work strictly top-down within a priority band. Acceptance criteria are testable on purpose.

## P0 — Pipeline core (no demo without these)

> **VIABILITY SPIKE (run FIRST, go/no-go before full build):** execute a minimal T-02 → T-03 → T-04 on ONE active Mantle contract: pull ~2,000 real txs, build the HDC prototype on the first 80% (warm-up), compute drift on the held-out 20% clean traffic, then inject one S1 selector flood.
> **Pass:** clean-traffic drift p99 < 0.3 with 0 false alerts AND injected drift > 0.7 within 2 windows (clear separation). Report the two numbers + a drift plot in LOG.md at the **June 14 12:00 Minsk checkpoint**. Pass ⇒ proceed to the full pipeline. Fail ⇒ adjust features/normalization or escalate before investing further. (D-08)


- [x] **T-01 Scaffold + CI** — package layout per AGENTS.md, `make check` (ruff + pytest), master seed config.
  *Accept:* `make check` green on empty test suite; `python -m sentinel --version` works.
- [x] **T-02 Data capture** — script pulling N days of tx history for 1–3 Mantle contracts via RPC into `bench/data/{contract}/raw.jsonl` (schema in BENCHMARK_PROTOCOL §1.2) + SHA-256 manifest.
  *Accept:* snapshot for ≥1 active contract with ≥2,000 txs; rerun is idempotent.
- [x] **T-03 Tier 2: HDC encoder** — bipolar hypervectors, D=10,000 default (D-02); role-filler binding per feature (caller, selector, gas/value/timing buckets); **level encoding for ordinal buckets**; window bundling (W=50 default).
  *Accept:* unit tests — (a) same tx ⇒ identical vector; (b) adjacent gas buckets ⇒ similarity > random pair; (c) window bundle vs prototype similarity > single-tx vs prototype.
- [x] **T-04 Tier 3: drift signal** — Hamming(window-bundle, prototype) + timing deviation; `normalize()` = rolling median/MAD z-score → squash to [0,1]; `drift = max(...)` exactly as frozen.
  *Accept:* on a stationary synthetic stream drift stays < 0.3; on an injected selector flood drift > 0.7 within 2 windows.
- [x] **T-05 Tier 1: entropy filter** — per-selector, length-conditioned Shannon entropy baseline; hard threshold; bypasses Tier 4 per freeze.
  *Accept:* normal ABI calldata from snapshot ⇒ 0 alerts; randomized calldata bodies ⇒ alert.
- [x] **T-05b Tier 0: spam pre-filter** — MIN_INTERVAL rule per MVP_MATH_SPEC §0; spam txs excluded from baselines.
  *Accept:* synthetic burst (1000 tx, near-zero intervals) ⇒ one `spam_attack` episode and unchanged prototype.
- [x] **T-06 Tier 4: StaticThresholdDetector** — threshold + hysteresis (k consecutive windows), alert-episode collapsing (10-min).
  *Accept:* unit tests for trigger, hysteresis, episode merge.
- [x] **T-07 Tier 5: interpreter + Alert JSON** — per-feature similarity probe (unbind roles), name top drifting feature(s); emit alert per `contracts/alert.schema.json`.
  *Accept:* on each injected scenario the interpreter names the injected feature; JSON validates against schema.
- [x] **T-08 End-to-end replay** — `python -m sentinel replay --snapshot ... [--inject ...]`: transactions → Alert JSON. **This is the MVP goal line.**
  *Accept:* clean replay produces a finite alert list; injected replay produces ≥1 true alert with correct interpreter output.

## P1 — Evidence (what convinces judges; see docs/BENCHMARK_PROTOCOL.md)

- [x] **T-09 Injector** — scenarios S1 (selector flood), S3 (gas shift), S5 (timing burst), S7 (payload mutation); seeded.
- [x] **T-10 Scorer** — detection delay (blocks, median/p90, misses censored) + FP episodes/contract-day on clean replay.
- [x] **T-11 FreqBase baseline + matched-budget comparison** (BENCHMARK_PROTOCOL §4) — produces the AB3 table.
- [x] **T-12 `bench/REPORT.md`** — 3-number headline box + scenario table; auto-generated by `make bench`.
- [x] **T-19 Z.ai explanation layer** (moved from P2 — panel consensus) — alert-only, strict template: may only restate Tier 5 structured findings; pre-generate the demo explanation; mock in tests.
- [x] **T-21 Telegram alert delivery** — send Alert JSON summary via bot token (env var); `--dry-run` mode; mocked test, no live calls in CI.

## P0-SUBMIT — Submission package (parallel track, hard deadline June 15)

- [x] **T-13 Mantle contract address (DECIDED: mainnet)** — `SentinelAlertRegistry` (event-emitting alert anchor, ~30 lines Solidity). **Final deploy to Mantle MAINNET, done manually by the Project Lead from his own wallet** — the deploy key is NOT given to any agent. Agents prepare/verify the contract + deploy script; Lead runs the final deploy and pastes the address back. Pipeline writes one real alert hash to it for the submission.
- [x] **T-14 Demo video (≤3 min)** — recorded replay: clean traffic → injected incident → alert → interpreter explanation. Screen capture + voiceover/captions. Script in `docs/DEMO_SCRIPT.md` (write it first, 1 page).
- [x] **T-15 README for judges** — what/why/how-to-run in 1 screen, architecture diagram, headline numbers from T-12, partner-tool usage section (Z.ai explanation layer; Mantle RPC; note Z.ai is a judge-affiliated tool).
- [x] **T-16 DoraHacks BUIDL page + X thread draft** — pitch text, links, hashtag `#MantleAIHackathon`. Draft goes to Project Lead for posting; agents do not post socials.

## P0-DEMO — Live self-attack on testnet (scenario B, the hero video)

- [x] **T-13b Victim contract (testnet)** — deploy a small self-owned target contract to Mantle Sepolia testnet (funded via faucet). We attack only our OWN contract — never third-party. Deploy key is a throwaway testnet key.
  *Accept:* contract live on Sepolia explorer; address recorded in LOG.md.
- [x] **T-13c Self-attack script** — a "normal user" warm-up script then an anomaly script (selector flood / gas spike / mutated calldata) hitting T-13b on testnet; Sentinel reads the live RPC stream and raises an alert.
  *Accept:* live run produces a true alert → on-chain anchor write (testnet) → Telegram message → explorer tx hash; whole loop recorded for the demo video.

## P2 — Only if P0+P0-SUBMIT are done

- [x] **T-17 BOCPD detector** behind the same interface as T-06 (frozen Tier 4 option) + AB2 comparison.
- [x] **T-18 Dream Mode** — with λ parameterized relative to N + the Dream Mode unit test (V_new ≠ V_old; similarity floor); S6 slow-drift dual run.
- [x] **T-20 Frontend demo page** — render Alert JSON timeline from a replay run.

## Cut rule

If time runs out: T-17/T-18/T-20 are cut first, then T-21 and T-19 (keep the pre-generated demo explanation), then T-11/T-12 (keep T-09/T-10 raw numbers). Never cut T-08 or P0-SUBMIT.
