# DECISIONS.md — Spec-Gap Decision Log

Format: `D-NN | date | gap | choice | rationale` (one line each).

D-01 | 2026-06-13 | Tier 5 attribution method unspecified | Feature-ablation contribution (MVP_MATH_SPEC §5) | avoids unbinding complexity; testable per scenario
D-02 | 2026-06-13 | HDC dimension | D=10,000 (was 4,096 in first spec draft) | panel consensus; cost negligible at MVP scale
D-03 | 2026-06-13 | timing/drift normalization estimator | rolling median/MAD robust z (not mean/std) | resists outlier contamination of baseline; architecture unchanged
D-04 | 2026-06-13 | Dream Mode lambda | lambda = alpha*N, alpha=0.5 (was fixed lambda=50) | fixed lambda is a no-op for large N and erases memory for small N; panel allows tuning
D-05 | 2026-06-13 | conflicting GPT docs uploaded 06-12 | archived to docs/archive/gpt_drafts/, canonical docs restored/rewritten | they described different products; repo docs must be a single source of truth
D-06 | 2026-06-13 | alert schema | replaced generic IT-alert schema with drift-detector schema at contracts/alert.schema.json | matches MVP_MATH_SPEC §5 output and T-07
D-07 | 2026-06-13 | T-13 network choice | final SentinelAlertRegistry on Mantle MAINNET (manual deploy by Lead, key not shared with agents); self-attack + debug runs on Sepolia testnet | mainnet cost negligible (~$0.13 full run); testnet for the live attack avoids key risk and is free
D-08 | 2026-06-13 | de-risk before full build | viability spike (T-02->T-03->T-04 on 1 contract) as a go/no-go gate at the Jun 14 12:00 checkpoint, pass = clean p99<0.3 & 0 FP, injected>0.7 | core thesis = does HDC drift separate normal vs anomalous on real Mantle data at low FP; test it in ~2h not on day 5
D-09 | 2026-06-13 | normalize() MAD degeneracy | if 1.4826*MAD <= EPS (near-constant signal, >50% identical history) fall back to robust std of history before dividing | a tiny EPS divisor makes any deviation explode to drift=1.0 on a quiet contract; std fallback keeps the z finite without changing the estimator family (still D-03)
D-10 | 2026-06-13 | squash(z)=z/6 ⇒ instantaneous clean p99 ~0.4-0.6 (Gaussian p99≈2.33σ→0.39); literal "clean p99<0.3" not physically reachable for stochastic traffic | report viability-spike FP as Tier-4 detector episodes (θ + k=3 hysteresis ⇒ 0 false alerts on clean) plus the clean/injected separation ratio; keep squash frozen | FP control belongs to the hysteretic detector per ARCHITECTURE_FREEZE, not to a per-window drift cap; flag to Lead at the Jun 14 checkpoint
D-11 | 2026-06-13 | demo contract choice | USDC.e (0x09bc4e0d864854c6afb6eb9a9cdf58ac190d0df9) selected as primary demo/attack contract (5 clean FP episodes vs LB Router 14; separation 4.3x); LB Router remains backup | homogeneous ERC-20 yields lower FP under fixed theta/k; router heterogeneity inflates clean regime count, some of which are genuine behavioral shifts
D-12 | 2026-06-14 | Dream Mode "safe window" definition (MVP_MATH_SPEC §6 left it implicit) | safe = Tier-4 detector silent AND drift < running median over the last W=100 windows (rolling, NOT all-time); consolidate the drift prototype via dream_update(alpha=0.5) every n_dream=100 safe windows; any alert clears the pending safe-buffer | rolling median tracks the current benign regime so the buffer fills at a bounded rate and never folds attack/anomalous windows into the baseline; an all-time median would drift down on long clean streams and over-eagerly accept windows. Off by default (frozen behaviour); opt-in via dream_mode/--dream-mode (T-18d)
