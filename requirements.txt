# Mantle Sentinel — runtime + test dependencies
# (live on-chain extras live in pyproject.toml [project.optional-dependencies].live:
#  web3, eth-account — only needed for `self_attack.py --victim ... --rpc ...`)

numpy>=1.26
httpx>=0.27          # Z.ai (OpenAI-compatible) HTTP client — Tier 5 explainer
jsonschema>=4.0      # alert-schema validation (tests/test_interpreter.py)
pytest>=8.0          # test runner (CI: python -m pytest tests/ -q)
